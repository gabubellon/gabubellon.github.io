---
label: selector_template
hide_body: true
fields:
- type: field_group_list
  name: Other skills
  label: Other skills
  fields:
  - name: values
    type: list
    config:
      use_select: false
      min: 
      max: 
      source:
        type: simple
    label: values
